import React from 'react';

function Header() {
    return (
        <header className="navbar text-light bg-primary text-center font-weight-bold">
            <span>To Do List</span>
        </header>
    );
}

export default Header

